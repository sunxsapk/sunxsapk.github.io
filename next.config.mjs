/** @type {import('next').NextConfig} */
const buildConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/',
  assetPrefix: '/'
};

const localConfig = {};

export default buildConfig;
